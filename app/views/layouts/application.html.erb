<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Rails Bootstrap" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Bootstrap" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class='bs-docs-header'>
      <%= render 'layouts/navigation' %>
    </header>
    <div class='container'>
      <div class='row'>
        <main class='col-md-10' role="main">
           <%= render 'layouts/messages' %>
           <%= yield %>
        </main>
        <div class='col-md-2 bs-docs-sidebar' role="complementary">
           <%= content_for?(:side_bar) ? yield(:side_bar) : (render 'layouts/side_bar') %>
        </div>
      </div>
    </div>
  </body>
</html>
